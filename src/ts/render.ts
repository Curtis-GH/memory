// TODO Phase 1: renderHome(), Phase 2: renderSettings(), Phase 3/4: renderBoard(), Phase 5: renderGameOver()
// Bewusst noch leer - wird erst mit dem jeweiligen Screen gefuellt, um nichts zu erfinden.
export {};
